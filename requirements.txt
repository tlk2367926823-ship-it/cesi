# This project is deployed as a Vite + Netlify Functions app.
# No Python runtime dependencies are required for production deploys.
#
# Keep this file empty of packages so Netlify does not try to install
# legacy Python dependencies such as xhs, which is not available in the
# required version on PyPI.
